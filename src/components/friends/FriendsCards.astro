---
import FriendCard from "./FriendCard.astro";

import yml from "./config.yml";

interface Friend {
  name: string;
  section?: boolean;
  desc?: string;
  slogan?: string;
  url: string;
  avatar: string;
}
---

<ul class="flex flex-wrap mt-4">
  {
    yml.friends.map((friend: Friend) =>
      friend.section ? (
        <div class="basis-full mb-4 mx-4">
          <h3 class="my-2 mt-8">{friend.name}</h3>
          {friend.desc && (
            <p class="secondary-color">
              <i class="ri-double-quotes-l" />
              {friend.desc}
              <i class="ri-double-quotes-r" />
            </p>
          )}
        </div>
      ) : (
        <FriendCard
          name={friend.name}
          desc={friend.desc}
          slogan={friend.slogan}
          url={friend.url}
          avatar={friend.avatar}
        />
      ),
    )
  }
</ul>
